# Filesystem Module — Lesson Stub

## Concept

Filesystem hierarchy and key directories (`/etc`, `/var`, `/proc`, `/sys`, `/dev`).

## Guided Lab

1. Inspect `/etc` and find system configs.
2. Explore `/proc` for process information.

## Challenge

Fix the broken fstab entry and ensure the system boots cleanly.
